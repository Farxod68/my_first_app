/// Spacing scale for TOPBUY DEALS, in logical pixels.
///
/// Design-token foundation: these values match the spacing already used
/// (by convention, as inline literals) throughout the current screens.
/// Consumed across the app's screens and widgets.
class AppSpacing {
  AppSpacing._();

  static const double xs = 4;
  static const double sm = 8;
  static const double md = 12;
  static const double lg = 16;
  static const double xl = 20;
  static const double xxl = 24;
  static const double xxxl = 32;
  static const double xxxxl = 40;
}
